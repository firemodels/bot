#!/bin/bash
# build a test bundle using revision and tags defined in BUILD_config.sh .
# the bundle is uploaded to the github website with username firemodels and release test_bundles
./BUILD_release_bundle.sh test_bundles
